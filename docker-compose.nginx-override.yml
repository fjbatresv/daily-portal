services:
  backend:
    environment:
      SERVE_STATIC: 'false'
    ports: []

  nginx:
    image: nginx:1.29-alpine
    profiles:
      - nginx
    ports:
      - '${HOST_PORT:-8090}:80'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
